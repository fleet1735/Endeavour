{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Parallel Runner (as module)",
      "type": "python",
      "request": "launch",
      "module": "engines.parallel_runner",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}/01_src"
    }
  ]
}
