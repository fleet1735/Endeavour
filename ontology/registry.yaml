entities:
  SignalEvent:
    required: [ontology_version, entity, event_id, setup_ref, instrument_ref, ts_event, payload]
    properties: [ontology_version, entity, event_id, setup_ref, instrument_ref, universe_ref, calendar_ref, ts_event, ts_signal, payload, hash, source, version, links]
  Trade:
    required: [ontology_version, entity, trade_id, run_id, side, qty, price, ts_exec, signal_event_ref]
    properties: [ontology_version, entity, trade_id, run_id, position_id, side, qty, price, ts_exec, signal_event_ref, fees, tags]
  MetricSummary:
    required: [ontology_version, entity, run_id, totals]
    properties: [ontology_version, entity, run_id, setup_ref, engine_version, totals, hashes]